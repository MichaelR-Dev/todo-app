import React from 'react'

export default function NewTask() {

    const onClickNewTask = (e: React.MouseEvent<HTMLButtonElement>) => {
        
    }

  return (
    <button type="button" onClick={onClickNewTask}></button>
  )
}
